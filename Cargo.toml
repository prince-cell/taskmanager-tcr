[package]
name = "tasksmanager"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = "0.27"
tui = "0.19"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"


[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
